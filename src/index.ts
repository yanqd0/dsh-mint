/**
 * dsh-mint — DSH plugin that integrates the mint issue tracker into DSH sessions.
 *
 * Placeholder entry: the host-face features (context injection, event reminders,
 * plan binding, mint_query tool) land in issues #3–#6.
 */
export const version = '0.1.0-alpha.0';
